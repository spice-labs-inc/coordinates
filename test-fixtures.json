{
  "$comment": [
    "What Surveyor's integration tests take from this repository, at the commit its jar was built",
    "from (./build-surveyor expectations). tests[]: unit-test source roots; expectations[]: the data",
    "files that drive tests (their id field is the test ID); fixtures[]: paths to export by tier",
    "(0 committed and small, 1 nightly, 2 full corpora); downloads[]: fetched at the given tier."
  ],
  "schemaVersion": 1,
  "component": "coordinates",
  "tests": ["java/src/test/java", "ts/test", "rust/tests"],
  "expectations": [
    {
      "glob": "vectors/intrinsic.json",
      "format": "coordinates-intrinsic",
      "cases": "$.vectors[*]",
      "idField": "name"
    },
    {
      "glob": "vectors/purl-ns-rules.json",
      "format": "coordinates-purl",
      "cases": "$.tests[*]",
      "idField": "description"
    },
    {
      "glob": "vectors/extrinsic.json",
      "format": "coordinates-purl",
      "cases": "$.tests[*]"
    },
    {
      "glob": "vectors/purl-types.json",
      "format": "coordinates-purl",
      "cases": "$.tests[*]"
    }
  ],
  "fixtures": [
    {
      "path": "vectors",
      "tier": 0
    }
  ],
  "downloads": []
}
